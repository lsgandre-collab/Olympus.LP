# Resend (waitlist emails). Get API key at https://resend.com
# If not set, signups are only logged to the server console.
RESEND_API_KEY=
WAITLIST_EMAIL=

# Optional: "From" address (must be a verified domain in Resend). Default: onboarding@resend.dev
# RESEND_FROM_EMAIL=waitlist@seudominio.com

# ARES Backend — Keepa API (server-side only, NEVER expose to client)
# Get your API key at https://keepa.com/#!api
KEEPA_API_KEY=
